<%- include('header.ejs'); %>
<div class="page-content">
    <div class="container-fluid">
        <div class="page-header d-flex justify-content-between align-items-center">
            <div>
                <h1 class="h3 mb-1">Pending Enquiries</h1>
                <p class="text-muted small mb-0">Review new contact form submissions.</p>
            </div>
        </div>

        <div class="card">
            <div class="card-body p-0">
                <% if (enquiries.length === 0) { %>
                    <div class="p-4 text-muted">No pending enquiries right now.</div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Subject</th>
                                    <th>Message</th>
                                    <th>Received</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% enquiries.forEach(function(item){ %>
                                    <tr>
                                        <td><%= item.name %></td>
                                        <td><%= item.email %></td>
                                        <td><%= item.subject || '-' %></td>
                                        <td><%= item.message %></td>
                                        <td><%= new Date(item.created_at).toLocaleString() %></td>
                                        <td>
                                            <a href="/admin/enquiries/accept/<%= item.id %>" class="btn btn-success btn-sm">Accept</a>
                                            <a href="/admin/enquiries/reject/<%= item.id %>" class="btn btn-danger btn-sm">Reject</a>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>
<%- include('footer.ejs'); %>
